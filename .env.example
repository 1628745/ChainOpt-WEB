# Track A — Supabase (SDK logging + calls-table tests)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-or-service-role-key

# Track B — Claude static analyzer (`chainopt analyze`)
# Create a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: override the analyzer model (default below)
# CHAINOPT_ANALYZE_MODEL=claude-sonnet-4-20250514
